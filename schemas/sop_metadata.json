{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://kalitta.com/schemas/sop_metadata.json",
  "title": "SOP Metadata Schema",
  "description": "JSON Schema for Standard Operating Procedure (SOP) YAML front matter metadata",
  "type": "object",
  "required": [
    "title",
    "category",
    "document_id",
    "owner",
    "approver",
    "effective_date",
    "status",
    "department",
    "revision"
  ],
  "properties": {
    "title": {
      "type": "string",
      "minLength": 3,
      "maxLength": 200,
      "description": "Document title"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Document tags for categorization"
    },
    "category": {
      "type": "string",
      "enum": ["SOP"],
      "description": "Document category (must be SOP)"
    },
    "document_id": {
      "type": "string",
      "pattern": "^SOP-[0-9]{3,4}$",
      "description": "Document ID in format SOP-###"
    },
    "owner": {
      "type": "string",
      "minLength": 2,
      "description": "Document owner (author/maintainer)"
    },
    "approver": {
      "type": ["string", "null"],
      "minLength": 2,
      "description": "Document approver"
    },
    "effective_date": {
      "type": ["string", "null"],
      "pattern": "^([0-9]{4}-[0-9]{2}-[0-9]{2})?$",
      "description": "Effective date in YYYY-MM-DD format"
    },
    "status": {
      "type": "string",
      "enum": ["Draft", "Review", "Active", "Retired"],
      "description": "Document lifecycle status"
    },
    "amos_version": {
      "type": "string",
      "description": "AMOS version this document applies to"
    },
    "apn": {
      "type": "string",
      "description": "AMOS APN number"
    },
    "downstream_apn": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of downstream APN dependencies"
    },
    "upstream_apn": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of upstream APN dependencies"
    },
    "department": {
      "type": "string",
      "minLength": 2,
      "description": "Department responsible for this document"
    },
    "pipeline_profile": {
      "type": "string",
      "description": "Pipeline processing profile"
    },
    "revision": {
      "type": "integer",
      "minimum": 0,
      "description": "Document revision number"
    }
  },
  "additionalProperties": true
}
